import comp from './comp.vue'

const compConfig = {
  name: 'dwxmqk',
  component: comp,
  thumbnail: {
    type: 'text',
    value: "单位泄密情况"
  },
  config: {
    basic: {
     
    },
    props: {
     
    },
    events: {
   
    }
  }
}

export default compConfig