import comp from './comp.vue'

const compConfig = {
  name: 'overView',
  component: comp,
  thumbnail: {
    type: 'text',
    value: "总览"
  },
  config: {
    basic: {
     
    },
    props: {
     
    },
    events: {
   
    }
  }
}

export default compConfig