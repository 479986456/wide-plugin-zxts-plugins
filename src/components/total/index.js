import comp from './comp.vue'

const compConfig = {
  name: 'total',
  component: comp,
  thumbnail: {
    type: 'text',
    value: "总量"
  },
  config: {
    basic: {
     
    },
    props: {
     
    },
    events: {
   
    }
  }
}

export default compConfig